// This file is intentionally left with a simple export to align with the new client setup.
// We are now centralizing client creation in `src/lib/supabase/client.ts`.
export { createClient, createAdminClient } from './client';
